describe "ResourceManager" do

  context "when dealing with yaml" do
    it "should resolve YAML file paths"
    it "should read YAML files to dicts"
    it "should write dicts to YAML files"
  end

  context "when dealing with puppet" do
    it "should resolve manifest file paths"
    it "should read Puppet files to Templates"
  end

end
